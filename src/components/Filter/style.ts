import styled from 'styled-components'

export const StyledCanvas = styled.canvas`
  position: relative;
  width: 100%;
  border-radius: 10px;
  aspect-ratio: 16 / 9;
  z-index: 1000;
  object-fit: cover;
`
